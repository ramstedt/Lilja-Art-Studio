export const theme = {
  colors: {
    primary: process.env.SANITY_COLOR || '#3b82f6',
  },
  fonts: {
    body: '-apple-system, "Helvetica Neue", Arial, sans-serif',
    mono: 'Menlo, Monaco, monospace',
  },
  // more styles goes here
};
